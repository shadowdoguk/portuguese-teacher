// Test setup — extends vitest with @testing-library/jest-dom matchers
// (toBeInTheDocument, toHaveTextContent, etc.). Loaded via
// vitest.config.ts setupFiles.

import '@testing-library/jest-dom/vitest';